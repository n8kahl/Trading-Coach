[phases.setup]
# Ensure Node.js (with npm) is available for the web/ build step on Railway
nixPkgs = ["nodejs_20"]

[phases.build]
# Build the React front‑end (if present) into frontend_dist/
cmds = [
  "if [ -d web ]; then npm --prefix web ci && npm --prefix web run build; fi"
]

