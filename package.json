{
  "name": "trading-coach",
  "private": true,
  "description": "Root package for coordinating Next.js frontend builds.",
  "scripts": {
    "build": "npm run build:coach",
    "build:coach": "npm --prefix trade-coach-ui ci && npm --prefix trade-coach-ui run build",
    "start": "npm run start:coach",
    "start:coach": "npm --prefix trade-coach-ui run start -- --hostname 0.0.0.0 --port ${PORT:-3000}",
    "dev": "npm --prefix trade-coach-ui run dev",
    "test": "pytest -q",
    "test:ui": "npm --prefix trade-coach-ui test"
  }
}
